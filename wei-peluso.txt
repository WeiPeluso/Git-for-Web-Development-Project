Completed Codepen: https://codepen.io/wei-peluso/pen/XWmrWve?editors=1100

    1. What is Semantic HTML? 

    Semantic HTML describes its meaning to both the browser and developer.

    2. What is HTML used for? 

     HTML is used to create pages that are displayed on the World Wide Web.

    3. What is an attribute and where do we put it? 

    Attributes define additional characteristics or properties of the element.
    inside a tag and right after tag name.

    4. What is the h1 tag used for? How many times should I use it on a page?

    h1 standing for header 1, and it is used for a header and which is the most important to your page.
    usually we stick with one h1 on one page , but multiple h1 tags can be added.

    5. Name two tags that have required attributes

    <img src="">, <a href="">

    6. What do we put in the head of our HTML document? 

    The <head> tag contains metadata (document title, character set, styles, links, scripts), specific information about the web page that is not displayed to the user.

    7. What is an id? 
     
     an id is a name gived to uniquely identify an HTML element.

    8. What elements can I add an id to? 

    You can add id to any HTML element.

    9. How many times can I use the same id on a page? 

      you can only use it once, because it is unique.

    10. What is a class?
        A class attribute is used to define equal styles for elements with the same class name.

    11. What elements can I add a class to? 
 
      The class atrribute can used on any elements.

    12. How many times can I use the same class on a page? 
 
      As many as you need to.

    13. How do I get my link to open in a new tab?

     We can use the attribute target="_blank". 
     for example <a href="google.com" target="_blank">.
    
    14. What is the alt attribute in the image tag used for? 

       if an image cannot be display, then display the value of alt attribute.

    15. How do I reference an id?

      #

    16. What is the difference between a section and a div?

    A section tag is used to sections in a document like chapters, headers, footers.
    A div tag defines a division in an HTML document.

    17. What is CSS used for? 

    CSS is used for styling HTML document.

    18. How to we select an element? Example - every h2 on the page

    selectors are patterns used to select the element(s) you want to style.

    h2{

    }

    19. What is the difference between a class and an id? - Give me an example of when I might use each one

     You can use the same class on multiple elements also you can have multiple classes on the same element.
     But id is unique, each element can have only one id and each page can have only one element with that id.

     for example you can have 5 different paragraphs describe 5 different fruit, you can add class "fruit" to all five p, 
     so you can set the font size and other properties, but if you want to give each p a different color according to the fruit, for example, for banana, you want it to be yellow text, you can give the banana paragraph a ID="banana", so you can set it uniquely.

    20. How do we select classes in CSS?
        .className{}, 
    21. How do we select a p element with a single class of “human””?
      
       p.human{}

    22. What is a parent child selector? When would this be useful? 
    
        a parent child seletor used to select elements with a specific parent.
        When the elements have inheritance relationships,


    23. How do you select all links within a div with the class of sidebar?

        .siderbar a {

       }

    24. What is a pseudo selector?

       A pseudo selector selects elements that are in a specific state

    25. What do we use the change the spacing between lines?

         line-height: 1.8;

    26. What do we use to change the spacing between letters?
         
          letter-spacing: 3px;

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
       *{
           text-transform: capitalize;
           text-transform: lowercase;
            text-transform: uppercase;
       }

    28. How do I add a 1px border around my div that is dotted and black?
     
     div{    
         border: 1px dotted black;
        }
     
    29. How do I select everything on the page? 

       *{}

    30. How do I write a comment in CSS?
  
     single line comment //
     block comment /* comment */

    31. How do I find out what file I am in, when I am using the command line? 
        
        pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
         
         ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 

         rm filename, because it won't promot a dialogue to confirm the deletion and it cann't be reversed.

    34. Why should I use version control? 

       Because it allows you to keep track of changes made to your code over time, and you can revert back to specific 'versions' of your code.

    35. How often should I commit to github?
        
        everytime make a big change or have finished some functions.

    36. What is the command we would use to push our repo up to github? 

         git push -u origin wei-peluso

    37. Walk me through Lambda's git flow. 

    Step 1 : Fork, create your own version of repo.
    Step 2 : Add your TL as a collabrator on your forked Repo.
    Step 3 : Clone your Repo  git clone url
    Step 4 : cd to the directory
    Step 5 : create branch git checkout -b 'wei-peluso'
    Step 6 : work on your project
    Step 7 : add and commit
    Step 8 : push, git push -u origin wei-peluso
    Step 9 : Pull request, Add your TL to the Reviews and submit the pull request
     

     
       

Stretch Questions

    1. What is the difference between an inline element and a block element?
    
        An inline element does not start on a new line and only takes up as much width as necessary.
       A block element always starts on a new line and takes up the full width available.

    2. What happens when an element is positioned absolutely? 

          an element is positioned abolutely meanns the value of the element is absolute or fixed. 

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
      
      we can use padding


    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default.
        
       3 elements that are diplay block by default:  div, section, li
       2 elements that are display inline by default: span, br
       1 element that is display inline-block by default: button

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
 
     box model include margin, border,padding and content,

     we neeed understand how to calculate the box size, it iwll be (width + left & right padding + left &r ight border +left &right margin)
      